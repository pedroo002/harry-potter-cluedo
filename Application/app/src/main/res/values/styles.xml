<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.AppCompat.Light.NoActionBar.FullScreen" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="Widget.Button.HarryPotterButtonStyle" parent="android:style/Widget.Button">
        <item name="android:width">120dp</item>
        <item name="android:height">35dp</item>
        <item name="backgroundTint">@color/buttonBackground</item>
        <item name="android:textColor">@color/buttonText</item>
        <item name="android:textStyle">bold</item>
        <item name="borderWidth">2dp</item>
        <item name="android:background">@drawable/button_enabled</item>
    </style>

    <style name="Theme.AppCompat.Light.NoActionBar.FullScreen.CluedoStyle" parent="@style/Theme.AppCompat.Light.NoActionBar.FullScreen">
        <item name="android:buttonStyle">@style/Widget.Button.HarryPotterButtonStyle</item>
    </style>

</resources>
